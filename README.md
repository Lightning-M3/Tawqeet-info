# بوت توقيت (Tawqeet Bot)

بوت "توقيت" هو نظام متكامل لإدارة الحضور والانصراف في سيرفرات ديسكورد، مصمم خصيصاً لخدمة المجتمعات العربية بتوقيت مكة المكرمة. يوفر البوت أدوات دقيقة لحساب ساعات تواجد الأعضاء، إصدار تقارير دورية، ولوحات تحكم حية.

## ✨ المميزات الرئيسية

*   **تسجيل دقيق:** تسجيل دخول وخروج الأعضاء وحساب المدة بدقة بالثواني.
*   **تقارير آلية:** تقارير يومية تجدول تلقائياً الساعة 11:55 مساءً بتوقيت مكة المكرمة.
*   **تقارير إدارية:** إمكانية استخراج تقارير أسبوعية وشهرية (للشهر الحالي أو الأشهر السابقة).
*   **لوحة تحكم حية:** رسالة تفاعلية تعرض المتواجدين حالياً وتحدث تلقائياً.
*   **نظام المتصدرين:** قائمة المتصدرين (Leaderboard) لأكثر الأعضاء تفاعلاً.
*   **حفظ البيانات:** يتم الاحتفاظ بسجلات الحضور لمدة 12 شهراً للمراجعة والتدقيق.

## 🚀 خطوات التثبيت والتهيئة

بعد إضافة البوت إلى السيرفر الخاص بك، اتبع الخطوات التالية لتفعيله:

### 1. الإعداد الأولي (مطلوب)
استخدم الأمر التالي لربط البوت بقنوات ورتب السيرفر:
```
/setup configure
```
ستظهر لك خيارات لملئها:
*   `admin_role`: رتبة الإدارة التي يحق لها استخدام أوامر التحكم (مثل تعديل الوقت أو فرض الخروج).
*   `reports_channel`: القناة التي سيتم إرسال التقارير اليومية التلقائية إليها.
*   `logs_channel`: القناة التي سيتم إرسال سجلات الدخول والخروج اللحظية إليها.
*   `member_role` (اختياري): رتبة الأعضاء المسموح لهم بتسجيل الحضور (إذا لم تحددها، سيتاح للجميع).
*   `commands_log_channel` (اختياري): قناة سرية لمراقبة الأوامر التي يتم استخدامها.

### 2. تفعيل لوحة الجلسات النشطة (اختياري)
لإنشاء رسالة مثبتة تعرض الأعضاء المتواجدين حالياً في الجلسة:
1.  أنشئ قناة خاصة لهذه اللوحة (مثلاً: `#المتواجدين-الآن`).
2.  اكتب الأمر التالي في تلك القناة:
```
/activesessions
```
سيقوم البوت بإرسال رسالة وتحديثها باستمرار عند دخول أو خروج أي عضو.

## 📋 أوامر تهمك

### للأعضاء:
*   `/mystats`: عرض إحصائياتك الشخصية (ساعات اليوم، الأسبوع، الشهر).

### للإدارة:
*   `/monthlyreport`: استخراج تقرير حضور شهري (يمكن تحديد الشهر والسنة).
*   `/weeklyreport`: استخراج تقرير أسبوعي.
*   `/dailyreport` : استخراج تقرير يومي.
*   `/leaderboard`: عرض قائمة المتصدرين في السيرفر.
*   `/memberreport`: استخراج تقرير مفصل لعضو معين.
*   `/forcecheckin` & `/forcecheckout`: تسجيل دخول/خروج إجباري لعضو.
*   `/modifytime`: تعديل وقت حضور عضو يدوياً (إضافة أو خصم).
*   `/reset`: تصفير بيانات الحضور (تحذير: هذا الأمر يحذف البيانات).

## 📞 الدعم الفني
في حال واجهت أي مشكلة أو كان لديك استفسار، يمكنك الذهاب لسيرفر الدعم الفني الخاص بالبوت: https://discord.gg/haxPuBDJwv
